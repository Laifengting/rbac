<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns = "http://www.springframework.org/schema/beans"
       xmlns:xsi = "http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context = "http://www.springframework.org/schema/context"
       xmlns:mvc = "http://www.springframework.org/schema/mvc"
       xsi:schemaLocation = "http://www.springframework.org/schema/beans
	   http://www.springframework.org/schema/beans/spring-beans.xsd
	   http://www.springframework.org/schema/context
	   http://www.springframework.org/schema/context/spring-context.xsd
	   http://www.springframework.org/schema/mvc
	   http://www.springframework.org/schema/mvc/spring-mvc-4.0.xsd">
    <!-- SpringMVC 只是控制网站跳转逻辑 -->
    <!--
	配置包扫描路径
		只扫描控制器
	-->
    <context:component-scan base-package = "com.lft.rbac"
                            use-default-filters = "false">
        <context:include-filter type = "annotation"
                                expression = "org.springframework.stereotype.Controller"/>
    </context:component-scan>

    <!-- 视图解析器 -->
    <bean class = "org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name = "prefix"
                  value = "/WEB-INF/views/"/>
        <property name = "suffix"
                  value = ".jsp"/>
    </bean>

    <!-- 正确处理静态资源 -->
    <mvc:default-servlet-handler/>

    <!-- 处理动态资源 -->
    <mvc:annotation-driven/>

    <mvc:interceptors>
        <!-- 登录验证拦截器 -->
        <mvc:interceptor>
            <mvc:mapping path = "/**"/>
            <mvc:exclude-mapping path = "/login"/>
            <mvc:exclude-mapping path = "/doAjaxLogin"/>
            <mvc:exclude-mapping path = "/static/**"/>
            <bean class = "com.lft.rbac.interceptor.LogInterceptor"/>
        </mvc:interceptor>
        <!-- 授权验证拦截器 -->
        <mvc:interceptor>
            <mvc:mapping path = "/**"/>
            <mvc:exclude-mapping path = "/login"/>
            <mvc:exclude-mapping path = "/doAjaxLogin"/>
            <mvc:exclude-mapping path = "/static/**"/>
            <bean class = "com.lft.rbac.interceptor.AuthInterceptor"/>
        </mvc:interceptor>
    </mvc:interceptors>

    <!-- 文件上传解析器 -->
    <bean id = "multipartResolver"
          class = "org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <!-- 最大上传大小 单位是 Byte 2097152 byte = 2MB -->
        <property name = "maxUploadSize"
                  value = "2097152"/>
        <!-- 延迟解析 -->
        <property name = "resolveLazily"
                  value = "true"/>
    </bean>

    <!--<bean id = "logInterceptor"-->
    <!--      class = "com.lft.rbac.interceptor.LogInterceptor"/>-->

</beans>
